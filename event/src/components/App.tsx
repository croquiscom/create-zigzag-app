import React, { useContext, useCallback } from 'react';
import Context from './context/appReducerContext';

const App = () => {
  return(
    <div>
      HelloWorld
    </div>
  )
}

export default App;